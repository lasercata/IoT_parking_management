# Frontend
This is the code of the user interface for the IoT project.

## Setup
### Python setup
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### Run
```
# Each time
source .venv/bin/activate

python app.py

# Or with port specified
python app.py 3000
```
